/** @type {import('next').NextConfig} */
const nextConfig = {
        images: {
          domains: ['i.pravatar.cc', 'cdn.sanity.io'], // Allow images from the Sanity CDNn
        },
      
};
export default nextConfig;
